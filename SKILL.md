---
name: image-ppt
description: 将文字、PDF、报告和参考图片制作成图片版或可编辑 PPTX，或将已有幻灯片图片拆分还原为独立文字、形状、图表、表格与图片对象。适用于 AI 生图 PPT、读书分享、组会汇报、路演和图片转可编辑 PPT；不用于与演示文稿无关的普通图片编辑。
---

# 图片 PPT · 2.0

用图像模型创造视觉，用结构化场景保存内容，用原生 PowerPoint 对象交付编辑能力。
推荐 GPT Image 2.5；模型选择和工具边界见 [models.md](references/models.md)。
本技能不要求安装其他 PPT 技能，也不自动安装模型或调用收费 API。

## 路由

| 用户目标 | 路线 | 交付 |
|---|---|---|
| 新材料做 PPT，未限定图片版 | 编辑优先（默认） | 原生信息层 + 独立视觉素材 |
| 明确只要图片版、海报式演示 | 图片优先 | 有序图片 + 图片版 PPTX |
| 现有图片/PDF 页面还原、每个元素可编辑 | 图片重建 | 场景清单 + 分层素材 + 可编辑 PPTX |
| 已有可编辑 PPTX，只修改内容 | 保留原生结构 | 按需使用当前环境的 PPTX 编辑工具；不要先拍平成图片 |

“编辑优先”先决定哪些信息需要原生编辑，再生成无字背景和独立素材；无需先生成整页再逆向还原。
“图片重建”保留原图可见内容、比例和排版，用户未要求时不重新设计。

## 需求与进度

先读可用材料，复用用户已经提供的场景、页数、样式和输出偏好。
缺少源文件或影响事实准确性的要求时只问必要问题；一般风格和页数可结合材料给出明确假设并继续。
用户要四套方案时分别生成四张风格总览，保持同组页面和内容，并等待选择；
没有指定选型流程时，先做一页代表性样张校准，再继续全稿。用户说直接做或授权代选时直接推进。

在任务工作目录维护 [deck-spec.md](references/deck-spec-template.md)，记录页序、准确内容、字体/颜色、当前步骤、素材、未解决项和输出。
工作目录与技能安装目录分离。脚本路径从本 SKILL.md 所在目录解析，输入输出使用实际任务路径。
恢复任务先读规格和 scene.json；新指令只修改受影响的页与元素，已验收素材尽量复用。

## 1. 内容和视觉系统

- 确定每页主题、正文、数据与来源。图表数值、单位、人名、日期以材料为准。
- 默认 16:9；重建时优先保留原比例。混合比例先明确统一画布及留白方案。
- 锁定字体、字号梯度、配色、页边距、图像风格和图表格式；不要机械套用同一种卡片网格。
- 将需要修改的标题、正文、标签、数字规划为文本框，表格与可验证的数据图规划为原生对象。
- 先测试内容最密或结构最复杂的一页，再批量处理。提示词见 [prompts.md](references/prompts.md)。

## 2. 生图和资产

在可用生图工具中先检查其输入参数。工具不暴露模型选择时，不宣称指定了 Sunburst/Flare。
模型版本已锁定时保持用户选择。尺寸、透明背景、参考编辑策略见 [models.md](references/models.md)。

编辑优先：分别生成无字视觉背景、人物/插画/产品素材。不要把正文、数据标签或原生图表画进背景。
图片优先：逐页生成成品图片并核对文字；错字修复可用原有 overlay_text.py。
已有图片重建：先逐页识别再制备资产，详细规则见 [reconstruction.md](references/reconstruction.md)。

每个需要独立移动的视觉对象对应独立图片对象；复杂插画内部仍是像素，不能承诺每根线条都能编辑。
重叠对象需要分层与背景补全，简单矩形裁剪不能自动分离它们。
原始页面只作为核对证据，不得作为“可编辑版”的整页底图保留。

## 3. 元素场景与原生导出

用 [scene-format.md](references/scene-format.md) 和 [示例](examples/editable-scene.example.json) 编写 scene.json；
[JSON Schema](references/scene.schema.json) 是机器契约。识别由宿主视觉模型/Agent 执行，本地脚本不会自行理解图片。

场景至少覆盖每页可见的语义元素，包含稳定 ID、位置、层级、准确文字和素材路径。
对低置信度识别、无法恢复的遮挡内容和复杂栅格对象记入规格，不能通过漏记元素获得“全可编辑”。

```sh
python "<skill-dir>/scripts/build_editable_ppt.py" "<work>/scene.json" "<work>/output/editable.pptx"
python "<skill-dir>/scripts/audit_editability.py" "<work>/output/editable.pptx" --scene "<work>/scene.json" --output "<work>/output/editability.json"
```

- 支持 text、shape、line、image、group、table、chart。组合可取消组合，子元素各自保留 ID。
- 列表顺序决定从底到顶的叠放次序；分组子元素也使用整页坐标。
- 文字、几何、数据改 scene.json 后重新编译；复杂图片修改只重做对应素材。
- 全页 SVG 插入 PPTX 仍是图片，不等于原生元素导出。本版不提供通用 SVG 导入器。
- 图片版沿用 `build_image_ppt.py <images> <output.pptx>`。此路线不报告原生可编辑性。

## 4. 验收

1. 结构检查：运行 audit，检查页数、元素类型、文字、表格单元格、图表数据、层级与缺失对象。
2. 视觉检查：使用当前环境的 PowerPoint 或独立渲染器导出每页预览，与参考逐页对照。若无渲染器，说明“仅结构验证”，不能声称视觉通过。
3. 内容检查：核对每页所有可见字符串和数据，重点检查中文换行、字体替换、数字和图表轴标签。
4. 编辑检查：抽查移动一个主体、修改一段文字、编辑一组图表数据并重新渲染；确认没有旧文字/旧主体残影。
5. 有问题只修对应 scene 元素或资产并复查；连续两次修复无效则记录具体限制，不能无限重试或隐藏失败。

结构检查不等于视觉验收，也不能证明识别出了原图全部元素。
背景栅格/低置信度警告要逐项解释；`--strict` 可将警告视为失败，不能为了通过而删除证据。

## 交付

默认交付 editable.pptx、scene.json、引用的 assets/ 和 editability.json；另附实际完成的预览（若有）。
保留字体替代、原生对象范围、独立栅格对象和待人工处理项的简短说明。
公开打包前排除未要求分享的源文档、凭证和临时缓存。

## 参考和工具

- [重建与分层](references/reconstruction.md)：图片/PDF 转可编辑、遮挡、背景、素材提取。
- [模型与参数](references/models.md)：GPT Image 2.5、原生工具与 API 的区别。
- [提示词](references/prompts.md)：样张、素材、重建、局部修改。
- [方法调研](references/research.md)：PPT Master、banana-slides、PPTAgent 的来源与取舍。
- `extract_assets.py`：按人工/视觉模型提供的框与遮罩提取素材；不执行 OCR 或智能抠图。
- `overlay_text.py`：准确文字栅格叠加，仅用于图片版。
- `build_image_ppt.py`：兼容旧版图片合并 CLI。
