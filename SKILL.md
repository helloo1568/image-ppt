---
name: image-ppt
description: "将书籍、PDF、论文、报告或任意文字材料转化为图片版或可编辑 PowerPoint 的分阶段工作流。用于图片PPT、读书分享PPT、课堂/组会/项目汇报、比赛路演，以及用户要求用 AI 生图制作 PPT、先生成四套幻灯片浏览视图式风格缩略图、把图片版 PPT 还原为可编辑 PPTX 等任务。默认严格执行需求确认、四套风格缩略图、用户选型、完整生成和可选编辑化；只有用户明确授权时才可跳过交互阶段。别名：image-ppt、doc-to-editable-ppt。"
---

# 图片 PPT

用 AI 图像生成能力逐页绘制 PPT，再按用户需要交付图片版 PPTX 或继续还原为可编辑 PPTX。

## 强制执行规则

以下规则属于硬性流程约束，高于主动执行、合理推断、减少交互轮次或一次性完成任务等通用偏好。

1. 不得替用户推断缺失的参考风格、使用场景或页数预期。
2. 需求未确认完整前，不得读取源材料正文、调用图像生成工具或开始制作 PPT。
3. 默认必须生成四套不同风格的“幻灯片浏览视图”缩略图，并等待用户选择。每套缩略图是一张包含多页迷你幻灯片的总览图，不是单页缩小图，也不得把四种风格拼成一张 2×2 图片。
4. “帮我生成 PPT”“根据技能制作”“直接做一份 PPT”等普通任务表达不构成跳过授权。
5. 只有用户明确要求跳过某一步、授权代理代选，或指定只执行某个阶段时，才允许快进。
6. 用户要求只生成图片版时，在 Step 2 后停止；不得自动执行可编辑化。
7. 用户后续的新指令高于已有制作规格。发生冲突时先更新规格和状态，再继续执行，禁止沿用过期选择。

## 状态机

每次行动前先判断当前状态，只执行该状态允许的动作。

| 状态 | 进入条件 | 允许动作 | 禁止动作 |
|---|---|---|---|
| S0 需求未齐 | 四项需求至少一项缺失 | 只询问缺失项 | 读取正文、生图、制作 PPT |
| S1 需求已齐 | 四项需求均已明确 | 读取材料、提炼大纲、生成四套缩略图 | 生成完整 PPT |
| S2 等待选型 | 四套幻灯片浏览视图缩略图已展示 | 等待用户选择或明确授权代选 | 自行选型、继续生成 |
| S3 风格已定 | 用户选定风格或明确授权代选 | 生成完整图片版 PPT 并交付 | 自动开始可编辑化 |
| S4 编辑化获批 | 用户明确要求可编辑 PPTX | 执行逐页还原并交付 | 超出用户指定范围 |

## Step 0：确认需求

确认以下四项：

1. **源材料**：附件、文件路径、PDF、书籍或用户粘贴的文字。
2. **参考风格**：模板、参考图片、文字风格描述，或明确“没有参考风格”。
3. **使用场景**：课堂汇报、读书分享、组会、周例会、项目汇报、比赛路演等。
4. **页数预期**：明确页数、页数范围，或明确授权 AI 根据内容推荐。

执行要求：

- 用户已经明确提供的项目不得重复询问。
- 文件附件或明确文件路径可视为源材料已提供。
- “没有参考风格”和“不确定页数，请推荐”均属于有效回答。
- 一次只集中询问缺失项目，问题保持简短。
- 收到回答后重新检查四项是否齐全，再进入 S1。

建议询问格式：

```text
开始制作前，还需要确认：
1. 这份 PPT 用于什么场合？
2. 是否有参考风格？没有也可以。
3. 大约需要多少页？不确定的话可以由我推荐。
```

## 显式快进

用户可以明确跳过部分交互流程。仅接受语义清晰的授权，例如：

- “跳过四套缩略图，直接制作。”
- “你替我选择最合适的风格。”
- “不用再询问，缺失项由你决定。”
- “只制作图片版 PPT，不做可编辑还原。”
- “从现有图片版 PPT 开始做可编辑还原。”

按最小授权原则解释快进指令。用户只允许代选风格时，仍需完成其他未被跳过的步骤。

## 制作规格与恢复

PPT 制作通常跨越多个工具调用和多个对话轮次。进入 S1 后，在当前任务的临时工作目录中创建 `deck-spec.md`，使用 `references/deck-spec-template.md` 记录：

- 已确认需求和明确快进授权
- 当前状态 S0–S4
- 内容大纲、页码、准确数据和文件名
- 四套幻灯片浏览视图缩略图、用户选型和锁定的视觉规范
- 每页的生成、修正和验收状态
- 最终交付文件路径

Codex 环境优先使用 `work/deck-spec.md`。每次恢复任务、上下文压缩后继续工作或收到用户新指令时，先读取并更新该文件。用户最新指令始终优先，不得用旧规格覆盖新要求。

## Step 1：内容提炼与四套缩略图

进入条件：S1，或用户已经明确授权跳过尚缺的确认项。

1. 读取并理解源材料，提炼适合目标场景和页数的汇报大纲。
2. 每页只承载一个主要结论，避免直接搬运大段原文。
3. 创建或更新 `deck-spec.md`，先锁定页数、大纲、逐页核心结论和必须准确出现的数据。
4. 从完整大纲中选取同一组代表性页面用于风格比较。建议包含封面、核心数据页、内容页、图表或结构页、案例页和总结/行动页；总页数不超过 8 页时可展示全部页面。
5. 使用 `references/prompts.md` 中的 Prompt 1，分别生成四张横向风格缩略图。每张图模拟 PowerPoint“幻灯片浏览视图”，以 3×2、4×2 或相近网格排列 6–8 张迷你 16:9 幻灯片。
6. 四套缩略图必须使用相同的页面顺序、页面类型、标题和核心内容，只改变整套演示文稿的配色、字体气质、版式系统、图形语言和素材风格。
7. 每套缩略图内部只能出现一种统一风格，不得在同一张图里混合多套设计方向。
8. 将四张风格缩略图分别编号为 1–4 并展示，简述各方案特点。不得额外拼成一张包含四种风格的 2×2 总览。
9. 进入 S2 并停止，等待用户选择。

若用户已明确授权代理代选，展示四套缩略图后可选择最适合使用场景的一套，并说明选择理由，然后进入 Step 2。

## Step 2：生成图片版 PPT

进入条件：用户已选定风格，或已明确授权代理代选/跳过预览。

1. 将选定方案提炼为文字化视觉规范，写入 `deck-spec.md`。不得仅依赖预览图记忆风格。
2. 使用 `references/prompts.md` 中的 Prompt 2，逐页生成完整 PPT 页面图片。
3. 每次生成都包含锁定的视觉规范、该页准确内容和页码信息。
4. 按 `01-title.png`、`02-title.png` 的形式编号文件，并实时更新逐页状态。
5. 每页生成后检查画幅、文字、数据、裁切、溢出和风格一致性；验收一页再标记一页，发现问题只重做对应页面。
6. 生图模型连续两次无法准确生成文字时，不得交付错误页面。改用“生成无字视觉背景 + 程序化叠加准确文字 + 栅格化”的混合方式，或减少页面文字后重做。
7. 优先运行 `scripts/build_image_ppt.py <图片目录> <输出.pptx>`，按自然页序把图片嵌入 16:9 幻灯片。
8. 同时保留有序的逐页图片，便于检查或后续编辑化。
9. 重新打开生成的 PPTX，核对页数、页序和画幅后再交付。除非用户明确要求可编辑化，否则在此停止。

## Step 3：还原可编辑 PPTX

进入条件：用户明确要求可编辑 PPTX。完整操作提示词见 `references/prompts.md` 中的 Prompt 3。

1. 每次处理 1–3 页，识别复杂视觉资产层和可编辑信息层。
2. 照片、插画、复杂背景和复杂图表可保留为高清图片。
3. 标题、正文、关键数字、页码、简单形状和基础图表尽量使用 PPT 原生元素重建。
4. 覆盖图片中的原文字后再叠加可编辑文本，避免文字重影。
5. 渲染并对照原图，检查布局、字体、颜色、裁切和文本溢出，迭代至无明显差异。
6. 交付 16:9 可编辑 PPTX，并说明图片元素、可编辑元素、字体替代和近似重建情况。

## 质量要求

- 内容结构符合用户场景和页数，而不是机械套用固定目录。
- 所有数字、日期、人名和引用必须与源材料一致。
- 四套缩略图必须是真正的幻灯片浏览视图，并使用同一组页面进行比较，不得更换内容来制造差异。
- 页面比例统一为 16:9，除非用户明确指定其他比例。
- 逐页图片必须使用零填充编号，页数、文件数和 PPTX 幻灯片数必须一致。
- 不得因生图效果好看而接受错误文字、错误数据或未经授权的内容改写。
- 图片版 PPTX 必须能够在 PowerPoint 中正常打开。
- 最终只执行用户授权的阶段，不将“完整工作流”误解为必须执行全部三步。

## 资源

- `references/prompts.md`：三个阶段的可复用提示词。仅在进入对应状态后读取和使用。
- `references/deck-spec-template.md`：跨轮次制作规格和逐页状态模板。
- `scripts/build_image_ppt.py`：将自然排序的 PNG/JPEG 页面图片确定性合并为图片版 PPTX。

## 工具映射

按当前环境选择文档读取、图像生成和 PPTX 构建工具。Codex 环境优先使用可用的图像生成技能逐页绘制，并运行随附脚本拼装图片版 PPTX；编辑化仍可使用 `python-pptx` 或 `pptxgenjs`。外部生图工具若不支持图生图，应把选定预览提炼成稳定、可复用的文字风格规范，逐页重复使用。
