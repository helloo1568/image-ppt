---
name: image-ppt
description: 将书籍、PDF、论文、报告或文字材料按固定三阶段流程制作成图片版 PPT，并在用户明确要求时继续还原为可编辑 PPTX。适用于图片 PPT、读书分享、课堂/组会/项目汇报和比赛路演；默认严格执行需求确认、四套幻灯片浏览视图选型、逐页图片生成和可选的 Scene v1 可编辑重建，不适用于直接修改已有可编辑 PPTX。
---

# 图片 PPT · 2.0.1

本技能只有一条主流程：**内容提炼 → 图片 PPT 生成 → 可编辑 PPTX 还原**。
Scene v1、分层素材和原生对象编译只服务于第三阶段，不得把流程改写成“先做原生信息层”的编辑优先路线。

## 平台与生图后端

- **最佳推荐：Codex + GPT Image 2.5。** 在 Codex 暴露图像模型选择时，Step 1 的快速方案探索优先使用 GPT Image 2.5 Flare，Step 2 成品页及 Step 3 的精确参考编辑优先使用 GPT Image 2.5 Sunburst。
- Codex 只提供通用生图入口、未暴露型号时，直接调用 Codex 当前可用的图像生成能力；不得声称指定或实际使用了某个 2.5 子型号。
- 本技能也可在其他支持技能/指令文件的 Agent 中使用。执行 Step 1–3 时调用**该 Agent 自身可用的生图或图片编辑能力**，并按实际工具参数调整；不得假设存在 Codex 专属工具。
- 仓库内 Python 脚本不负责生图、OCR 或视觉理解，也不自动调用收费 API。只有用户明确提供并授权 API 接入时，才可改用外部图像 API。

## 强制流程契约

以下约束高于主动执行、减少交互、合理推断或一次性完成任务等通用偏好：

1. 每次行动前先判断状态，只执行状态表允许的动作。
2. 需求未齐时，只确认缺失项；可以检查文件是否存在和页数等元数据，不得阅读正文、提炼内容、生图或制作 PPT。
3. 新材料制作 PPT 必须先完成图片版。用户从一开始就要求可编辑版，表示已授权第三阶段，但不允许跳过图片版直接创作原生 PPTX。
4. 默认必须生成四张独立的“幻灯片浏览视图”风格总览并等待选择。它们使用同一组页面和内容；不得用四张单页、单张四宫格或纯文字方案代替。
5. “帮我做 PPT”“直接做一份”“按技能制作”不构成跳步授权。只有语义明确的“跳过预览”“你代选”“缺失项由你决定”“从现有图片开始还原”等指令才允许快进，且只在授权范围内快进。
6. 未授权可编辑化时，交付图片版后停止；只要求图片版时不得创建 scene.json 或可编辑 PPTX。
7. 用户的新指令与旧规格冲突时，先更新 `deck-spec.md` 的需求、状态和失效产物，再继续。不得沿用过期选型。
8. 不得把工具不可用、时间不足或模型文字错误当作改写流程的理由。使用规定的兜底方案；当前 Agent 没有生图能力时停在对应状态并说明缺失能力，不得用任意占位图冒充结果。

## 状态机

| 状态 | 进入条件 | 允许动作 | 必须停止于 |
|---|---|---|---|
| S0 需求未齐 | 五项需求至少一项未明确，且没有对应快进授权 | 只询问缺失项 | 用户补充或明确授权代定 |
| S1 内容与方案 | 五项需求齐全，或缺失项已获授权代定 | 阅读材料、提炼大纲、创建规格、生成四套风格总览 | 四套总览已展示 |
| S2 等待选型 | 四套总览已展示 | 接收选择/修改意见；若已授权代选则记录选择 | 风格明确锁定 |
| S3 图片版制作 | 用户选定风格，或明确跳过/授权代选 | 逐页生成、验收并合并图片版 PPTX | 图片版已验收交付；或进入已授权 S4 |
| S4 可编辑重建 | 用户明确要求可编辑 PPTX，或明确要求从已有页面图片/PDF 直接还原 | 识别、分层、编写 Scene、编译、审查和渲染核对 | 可编辑交付完成 |

已有页面图片或扫描版 PDF 要求还原时，可从 S4 开始；先确认输入范围、原比例和编辑目标。
已有可编辑 PPTX 只做内容修改时，不走本技能三阶段流程，应保留原生结构并使用当前环境的 PPTX 编辑能力。

## S0：确认五项需求

一次集中询问尚未明确的项目，已经提供的不得重复询问：

1. **源材料**：附件、路径、PDF、书籍或粘贴文字。
2. **参考风格**：模板/图片/文字描述，或明确“没有参考风格”。
3. **使用场景与受众**：课堂、读书分享、组会、周报、项目汇报、路演等。
4. **页数预期**：固定页数、范围，或明确授权根据内容推荐。
5. **交付范围**：只要图片版，还是图片版完成后继续还原可编辑版。

“没有参考风格”“页数由你推荐”“其余由你决定”都是有效授权，必须原样记录到规格中。

## 制作规格

进入 S1 或 S4 后，立即把 [规格模板](references/deck-spec-template.md) 复制到任务工作目录并填写为 `deck-spec.md`。
任务目录与技能安装目录分离；所有源文件、生成图片、Scene、素材、预览和交付物都写入任务目录。

每次恢复任务、上下文压缩、用户改需求或阶段转换时，先读取并更新规格。规格至少记录：

- 五项需求、明确快进授权、当前状态和下一道门禁；
- 完整页序、准确文字/数据及来源；
- 四套总览、用户选择和锁定的视觉规范；
- 每页生成、修复、渲染与验收状态；
- S4 的元素 ID、素材、低置信度项、审查结果和交付路径。

## Step 1：内容提炼与四套风格总览（S1 → S2）

进入本阶段后读取 [阶段提示词](references/prompts.md) 的 Prompt 1。

1. 阅读源材料，按使用场景和页数形成完整大纲。每页一个主要结论；数字、日期、人名、引语和单位以材料为准。
2. 把完整页序、标题、核心结论和必须准确出现的内容写入规格。
3. 从同一份大纲选择 6–8 个代表页；总页数不超过 8 页时展示全部页面。
4. 分别生成四张横向总览图。每张图模拟 PowerPoint 幻灯片浏览视图，内部保持一种统一风格；四套只改变视觉系统，不改变页面顺序、标题、数据或内容结构。
5. 独立保存并展示方案 1–4，简述差异，然后进入 S2 并停止。

用户授权代选时仍生成四套方案，再根据使用场景选择并记录理由后进入 S3；用户明确说“跳过四套预览”时才可不生成。

## Step 2：生成图片版 PPT（S3）

进入本阶段后读取 [阶段提示词](references/prompts.md) 的 Prompt 2，以及 [模型与参数](references/models.md)。

1. 把选定方案写成可复用的视觉规范：配色、字体气质、字号层级、网格、留白、图形语言、素材风格、页码和禁用项。
2. 按 `01-title.png`、`02-title.png` 的零填充序号逐页生成完整页面图片。每次提示必须同时包含锁定视觉规范、该页准确内容、页码和总页数。
3. 每页生成后立即检查 16:9/指定比例、文字、数据、裁切、溢出和跨页一致性；通过后才在规格中标记“已验收”。失败只重做对应页。
4. 生图连续两次无法准确生成同一页文字时，停止继续重试：生成无字视觉背景，再用 `scripts/overlay_text.py` 程序化叠加准确文字并栅格化。
5. 全部页面验收后运行：

```sh
python "<skill-dir>/scripts/build_image_ppt.py" "<work>/slides" "<work>/output/image-deck.pptx"
```

6. 重新打开或渲染 PPTX，核对页数、页序、画幅和文件可打开性。保留有序页面图片。
7. 未授权 S4 时交付并停止；已授权可编辑版时更新规格后进入 S4。

## Step 3：还原可编辑 PPTX（S4）

进入本阶段后必须读取 [重建与分层](references/reconstruction.md)、[Scene 协议](references/scene-format.md) 和 Prompt 3。

1. 以 Step 2 验收后的页面图片为视觉基准；直接还原任务则以用户提供的规范化页面为基准。保持页序、比例、可见内容和排版，不重新设计。
2. 每次处理 1–3 页，逐项识别文字、数据、几何、表格、图表、图片和遮挡，给每个可见语义元素稳定 ID、整页坐标、层级、来源和置信度。
3. 标题、正文、数字、页码、简单形状、线、表格及有可靠数据的图表使用原生对象；照片、人物和复杂插画拆为独立图片对象。每个需要单独移动的对象对应一个独立对象。
4. 从背景移除所有已原生化文字和已拆出的主体，并补全露出的背景。原始整页图片只作核对证据，不得作为“可编辑版”底图保留。
5. 按 [JSON Schema](references/scene.schema.json) 编写 `scene.json`，然后运行：

```sh
python "<skill-dir>/scripts/build_editable_ppt.py" "<work>/scene.json" "<work>/output/editable.pptx"
python "<skill-dir>/scripts/audit_editability.py" "<work>/output/editable.pptx" --scene "<work>/scene.json" --output "<work>/output/editability.json"
```

6. 实际渲染每页并与基准图并排核对；检查文字、数据、位置、层级、透明边缘、字体替代、残影和溢出。结构审查不能替代视觉验收。
7. 抽查移动一个主体、修改一段文字和编辑一组图表数据。问题只修对应 Scene 元素或素材；同一问题连续两次修复无效时，记录具体限制，不得隐藏失败。

## 交付契约

- **图片版**：`image-deck.pptx`、有序页面图片、实际完成的预览/检查说明。
- **可编辑版**：在图片版交付基础上增加 `editable.pptx`、`scene.json`、引用的 `assets/`、`editability.json` 和渲染预览。
- 说明字体替代、低置信度识别、AI 补全、独立栅格对象和待人工处理项。
- 不能把整页截图加少量文本框、整页 SVG 或未拆分背景称作“每个元素可编辑”。复杂插画内部仍是像素，不承诺每条曲线可编辑。
- 没有可用渲染器时写明“仅完成结构验证”，不得声称视觉验收通过。

## 资源路由

- [prompts.md](references/prompts.md)：只在进入对应 Step 后读取该阶段提示词。
- [models.md](references/models.md)：Step 2 生图，以及 Step 3 需要背景清理/主体分离时读取。
- [reconstruction.md](references/reconstruction.md)：仅 S4 读取。
- [scene-format.md](references/scene-format.md)：仅 S4 编写或修改 Scene 时读取。
- `extract_assets.py`：按已经确认的框和遮罩裁剪素材；不执行 OCR、识别或智能抠图。
- `overlay_text.py`：只作为 Step 2 图片页的准确文字兜底。
- `build_image_ppt.py`：只用于 Step 2 图片版合并。
