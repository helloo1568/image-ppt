# 阶段提示词

按任务读取相关部分，将花括号替换为真实内容。生成后仍需检查。

## 内容与样张

从 {材料} 中组织 {页数/范围} 页，面向 {受众}。
保持事实和数值，每页注明主题、正文、证据、适合的视觉。
输出一套配色/字体/页边距规范和一页代表性样张计划。
若用户要求四套风格，用同一组内容分别生成四张幻灯片浏览视图，不改变事实制造风格差异。

## 无字背景

为 {画布} 演示文稿制作无字背景。
风格：{锁定视觉规范}。构图：{布局与信息留白}。
只保留 {不能移动的背景内容}。不包含标题、正文、页码、数据标签或将独立绘制的 {主体列表}。
对比参考检查亮度、颜色、位置和留白，不加入新装饰。

## 独立主体

以 {原始参考图} 为唯一构图基准，提取/重建 {ID 与主体描述}。
保持主体身份、姿态、颜色、比例、画布位置和光照。
输出真正透明背景的 PNG，只保留该主体及归属它的 {阴影/效果}。
画布 {尺寸}，不要改变原位置或自动紧贴裁边；不添加文字、底板、棋盘格或其他物体。
若边界被遮挡，最小限度补全，记录补全区域，交付时说明。

## 背景清理

参考 {原始页}，移除 {全部原生重建文字、几何和独立主体清单}。
仅补全露出的背景，保持其他背景结构、纹理、光照、尺寸和配色。
不要重排布局、移动剩余物体、改写文字或增添内容。
后续主体图也从原始页生成，不从本次清理结果继续改。

## 图片识别到场景

检查 {原始页面}，按 scene-format.md 创建 scene.json。
逐项识别文字、数据、几何、图片和遮挡，给每项稳定 ID 与 canvas 像素坐标。
文字逐字转录；表格逐格录入；图表仅使用能验证的数值。
可编辑信息用 text/shape/line/table/chart，复杂视觉用独立 image。
group 表达语义组合，所有子项仍用整页坐标。数组顺序对应真实叠放。
保留 source、confidence、note。无法确定的内容写入规格的未解决项，不虚构或省略。
只引用实际存在的素材文件，不给完整原始页面添加背景图片元素。

## 局部修改

只修改 {slide-id}/{element-id} 的 {文字/颜色/几何/图表数据/素材}。
以现有 scene.json 和原始素材为基准，保留其余对象 ID、页序、样式和层级。
几何与文字修改不重新生图。图片内容变化只重做对应素材，再编译并审查受影响页面。
