name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: python -m pip install -r requirements.txt -r requirements-dev.txt
      - name: Lint
        run: ruff check .
      - name: Test
        run: python -m pytest tests/ -v
      - name: Validate Page Spec example
        run: python scripts/validate_page_spec.py examples/page-spec.example.json --strict
      - name: Validate installer registration
        run: |
          python install.py --client codex --home "${{ runner.temp }}/skill-home" --skip-deps
          python -c "import os; from pathlib import Path; p = Path(os.environ['RUNNER_TEMP']) / 'skill-home' / '.agents' / 'skills' / 'image-ppt' / 'SKILL.md'; assert p.is_file(), p"
